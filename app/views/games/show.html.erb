
<header class="pure-g">
  <div class="pure-u-1">
    <h1><%= @game.name %></h1>
    <%= link_to "Record a Game", new_game_result_path(@game), :class => "pure-button pure-button-primary" %>
  </div>
</header>

    </div><!-- close .pure-g-r -->
</div><!-- close .pure-u-1 -->

<div class="pure-g">
  <div class="pure-u-1">
    <% if @recent_results.any? %>
      <%= render :partial => 'shared/most_recent', :object => @recent_results.shift %>
    <% end %>
  </div>
</div>

<div class="pure-g-r">
  <div class="pure-u-5-6 container">

    <div class="pure-g" id="player-rank">
      <div class="pure-u-1-2">
      <% if @player_ratings.any? %>
      <h2>Top 10 Players</h2>
        <ol>
         <%= render :partial => 'rating', :collection => @player_ratings.limit(4) %>
        </ol>
      <% else %>
        <p>
          There are no ranked players.
        </p>
      <% end %>
      </div>
      <div class="pure-u-1-2">
        <% if @team_ratings.any? %>
        <h2>Top 10 Teams</h2>
        <ol>
         <%= render :partial => 'rating', :collection => @team_ratings.limit(4) %>
        </ol>
      <% else %>
        <p>
          There are no ranked players.
        </p>
      <% end %>
      </div>
    </div>
    
    <div class="pure-g" id="match-feed">
      <div class="pure-u-1">
        <h2>Match Feed</h2>
          <% if @recent_results.any? %>
            <%= render :partial => 'shared/game_feed', :object => @recent_results %>
          <% else %>
            <p>No matches have been recorded.</p>
          <% end %>
      </div>
    </div>
