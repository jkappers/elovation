<tr>
	<td><%= rating_counter + 1 %></td>
	<td>
  	<% rating.team.players.each do |player| %>
    	<%= image_tag(gravatar_url(player, :size => 40)) %>
    <% end %>
    <%= link_to rating.team.name, team_game_path(rating.team, @game) %>
	</td>
	<td><%= rating.value %></td>
	
</tr>
