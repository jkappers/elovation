
<div class="page-header">
  <%= link_to "Record a Match", new_game_result_path(@game), :class => "btn btn-primary" %>  
  <h1><%= @game.name %></h1>  
</div>

<div class="row">
  <div class="col-md-12 col-lg-12">
    <% if @recent_results.any? %>
      <%= render :partial => 'shared/most_recent', :object => @recent_results.shift %>
    <% end %>
  </div>
</div>

<div class="row">
  <div class="col-md-4 col-lg-4">
    <div class="panel panel-default">
      <div class="panel-heading">Recent Matches</div>
      <% if @recent_results.any? %>
        <%= render :partial => 'shared/game_feed', :object => @recent_results %>
      <% else %>
        <div class="panel-body">
          No matches have been recorded.
        </div>
      <% end %>
    </div>
  </div>
  <div class="col-md-4 col-lg-4">
    <div class="panel panel-default">
      <div class="panel-heading">Top Ranked Players</div>
      <% if @player_ratings.any? %>
        <table class="table">
          <thead>
            <tr>
              <th>#</th>              
              <th>Player</th>
              <th>Games Played</th>
              <th>Rating</th>                            
            </tr>
          </thead>
          <tbody>
            <%= render :partial => 'rating', :collection => @player_ratings.select(&:active?) %>
          </tbody>
        </table>
      <% else %>
        <div class="panel-body">
          There are no ranked players.
        </div>        
      <% end %>
    </div>    
  </div>
  <div class="col-md-4 col-lg-4">
    <div class="panel panel-default">
      <div class="panel-heading">Top Ranked Teams</div>
      <% if @team_ratings.any? %>
        <table class="table">
          <thead>
            <tr>
              <th>#</th>              
              <th>Team</th>
              <th>Games Played</th>
              <th>Rating</th>                            
            </tr>
          </thead>
          <tbody>
            <%= render :partial => 'rating', :collection => @team_ratings %>
          </tbody>
        </table>
      <% else %>
        <div class="panel-body">
          There are no ranked players.
        </div>        
      <% end %>
    </div>    
  </div>
</div>